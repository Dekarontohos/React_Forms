import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import { ValidationFormLayout } from "./Components/ValidationForm";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
	<React.StrictMode>
		<ValidationFormLayout />
	</React.StrictMode>,
);
